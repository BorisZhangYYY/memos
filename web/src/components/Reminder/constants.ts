// A standard Memo card is at most 672px wide. Its header reserves 128px for
// built-in actions; a worst-case localized date/time takes about 176px. Three
// 112px reminder pills plus gaps fit in the remaining header space.
export const MAX_REMINDERS_PER_MEMO = 3;
